<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="62701"
        android:entries="@array/pref_network_port_titles"
        android:entryValues="@array/pref_network_port_values"
        android:key="network_port"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_network_port"

        />

    <ListPreference
        android:defaultValue="30"
        android:entries="@array/pref_network_freq_titles"
        android:entryValues="@array/pref_network_freq_values"
        android:key="network_freq"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_network_freq"

        />
<!--
    <SwitchPreference
        android:defaultValue="true"
        android:key="example_switch"
        android:summary="@string/pref_description_social_recommendations"
        android:title="@string/pref_title_social_recommendations" />

    !- - NOTE: EditTextPreference accepts EditText attributes. - -
    !- - NOTE: EditTextPreference's summary should be set to its value by the activity code. - -
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/pref_default_display_name"
        android:inputType="textCapWords"
        android:key="example_text"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_display_name" />

    !- - NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. - -
    !- - NOTE: ListPreference's summary should be set to its value by the activity code. - -
    <ListPreference
        android:defaultValue="-1"
        android:entries="@array/pref_example_list_titles"
        android:entryValues="@array/pref_example_list_values"
        android:key="example_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_add_friends_to_messages" />
-->
</PreferenceScreen>
